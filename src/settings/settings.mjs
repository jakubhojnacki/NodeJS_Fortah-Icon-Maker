/**
 * @module "Settings" class
 * @description Class representing application settings
 */

"use strict";

import { SettingsBase } from "core-library";

export class Settings extends SettingsBase {
    

    constructor(pDiagnostics) {
        super(pDiagnostics);
    }
}